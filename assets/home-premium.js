const money=n=>new Intl.NumberFormat('en-US',{style:'currency',currency:'USD'}).format(n);function calcFees(){const el=document.getElementById('tradeAmount');if(!el)return;const v=Math.max(0,Number(el.value)||0);const base=v===0?0:Math.max(.95,v*.005);const withRef=base*.9;document.getElementById('feeBase').textContent=money(base);document.getElementById('feeAfter').textContent=money(withRef);document.getElementById('feeSave').textContent=money(base-withRef)}window.setAmt=v=>{const el=document.getElementById('tradeAmount');if(el){el.value=v;calcFees()}};calcFees();const io=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting)e.target.classList.add('in')}),{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>io.observe(el));const sticky=document.querySelector('.stickyCta');const onScroll=()=>{if(sticky)sticky.classList.toggle('show',scrollY>650)};addEventListener('scroll',onScroll,{passive:true});onScroll();const visual=document.querySelector('.heroVisual'),phone=document.querySelector('.phone'),ref=document.querySelector('.refCard');if(visual&&phone&&matchMedia('(pointer:fine)').matches){visual.addEventListener('pointermove',e=>{const r=visual.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;phone.style.transform=`rotateY(${-13+x*6}deg) rotateX(${6-y*5}deg) rotateZ(${2+x*1.5}deg) translate3d(${x*7}px,${y*5}px,0)`;if(ref)ref.style.transform=`rotateY(${8-x*4}deg) rotateZ(${-3-x}deg) translate3d(${-x*6}px,${-y*4}px,0)`});visual.addEventListener('pointerleave',()=>{phone.style.transform='rotateY(-13deg) rotateX(6deg) rotateZ(2deg)';if(ref)ref.style.transform='rotateY(8deg) rotateZ(-3deg)'})}